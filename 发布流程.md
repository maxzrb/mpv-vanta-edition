# MPV Vanta Edition 发布流程（强制）

> 本文件是本仓库发布操作的**唯一权威流程**。任何 agent（Codex、Claude 等）在执行“发布 / 打包 / 创建 GitHub Release”类任务前，必须先完整阅读本文件并逐项执行检查。
> 本文件的修订权归项目所有者（用户）。agent 不得自行修改本文件；遇到流程无法覆盖或“改动较大影响发布内容”的情况，必须停下来向用户汇报，由用户决定是否修正发布流程。
> 发布时必须检查现有发布流程是否覆盖全功能，禁止功能未包含在发布脚本和构建中就执行发布。

## 1. 适用范围

- 仓库：`https://github.com/maxzrb/mpv-vanta-edition.git`
- 发布目标：GitHub Releases（`vX.Y.Z` 标签 + 公开资产）
- 产物：01～05 五个公开增量包 + VantaInstaller 安装器；个人私用全量包仅本地保留

## 2. 版本号

- 格式：语义化版本 `X.Y.Z`，例如 `1.4.0`。
- 每次发布前与用户确认版本号；用户未指定时 agent 不得擅自定号。
- 版本号必须与标签 `vX.Y.Z`、包文件名、`version/版本迭代记录.md` 中的“当前版本”完全一致。

## 3. 发布前置检查（每项必须通过）

### 3.1 工作区与 Git 状态

- [ ] `git status --short --branch`：明确当前分支、领先/落后、未提交/未跟踪文件清单。
- [ ] `git fetch origin`：与远端同步，确认远端无未合并改动。
- [ ] 未提交改动必须按逻辑整理为提交（功能提交 → 构建记录提交 → 发布结果提交），禁止“发布时夹带未审阅改动”。
- [ ] 确认 `master` 与 `origin/master` 在发布提交后同步。

### 3.2 发布内容影响评估（大改动 Gate）

以下任一情况属于**“改动较大影响发布内容”**，agent 必须**停止发布**，把影响面整理清楚后向用户汇报，由用户决定是否修正发布流程或豁免：

- 公开包的数量、编号、命名或覆盖顺序变化（如 01→02→…→05 结构调整）；
- 新增、删除、拆分或合并公开包，或包之间内容边界变化；
- 构建脚本、打包入口或产物生成方式需要修改（`build-*.ps1`、7-Zip 参数、分卷规则等）；
- mpv 核心、运行时（Python、VapourSynth、7-Zip、Lua、VC 运行库等）或关键外部工具升级/更换；
- 大型资源增删或明显改变包体积（着色器集合、AI 模型、字体、完整运行时目录等）；
- 安装、升级、卸载、启动方式或 README 安装顺序变化；
- VantaInstaller 工具台的功能、界面、构建方式、版本或发布形态变化（影响附装资产）；
- 公开包中新增涉及第三方版权/许可边界的文件（个人专有文件、Steam 资源等）；**04 内置 `Lossless Scaling\Lossless.dll` 是项目所有者已确认的既定发布内容，不视为新增**；
- 产物需要超过 GitHub 单文件限制（>2GB）或新增分卷规则；
- 需要修改本发布流程文件本身。

普通功能、界面、配置、脚本、菜单改动**不触发**该 Gate，走标准流程。

### 3.3 文档与记录检查

- [ ] `docs/codex/STATUS.md`：发布前记录本次要发布的功能/改动清单、验证结果、Git 状态。
- [ ] `version/工作进度.md`：追加本次发布准备与结果条目（不覆盖历史）。
- [ ] `version/版本迭代记录.md`：新版本发布时更新“当前版本”一节，旧版本移入历史。
- [ ] `README.MD`：若安装顺序、包结构、下载方式变化，必须同步更新。

### 3.4 功能验证

- [ ] 完整配置真实播放无目标错误（mpv 日志无新增 error）。
- [ ] 涉及 Lua 脚本时逐文件通过语法检查（`luajit -e "loadfile(...)"` 或等价方式）。
- [ ] 菜单/脚本配置与 `script-opts` 一致，UTF-8 无 BOM、LF 换行。
- [ ] `git diff --check` 通过。

## 4. 构建

### 4.1 命令

在仓库根目录执行：

```powershell
.\build-all-packages.ps1 -Version X.Y.Z
```

需要同时生成个人私用全量包时追加 `-IncludePrivate`：

```powershell
.\build-all-packages.ps1 -Version X.Y.Z -IncludePrivate
```

`build-all-packages.ps1` 依次调用：

1. `build-release.ps1` → 01 Base、02 Extras
2. `build-fasterwhisper-public.ps1` → 03 Faster-Whisper 扩展包
3. `build-lsfg-public.ps1` → 04 LSFG 扩展包
4. `build-config-public.ps1` → 05 Config 包
5. `build-full-private.ps1`（仅 `-IncludePrivate`）→ 个人全量包

VantaInstaller 安装器**单独构建**（不随 mpv 包脚本），在仓库 `VantaInstaller/` 目录执行：

```powershell
dotnet publish src\Vanta.Installer\Vanta.Installer.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:EnableCompressionInSingleFile=true -o publish\win-x64
```

产物：`VantaInstaller\publish\win-x64\VantaInstaller.exe`（压缩单文件自包含，约 64MB，免 .NET 运行时）。

### 4.2 产物清单

| 包 | 内容 | 说明 |
|----|------|------|
| `01-mpv-base-vX.Y.Z.7z` | mpv 核心 + 运行时 + 基础配置 + 启动 Logo 素材（启动页/起播格式 Logo） | 首个安装 |
| `02-mpv-extras-vX.Y.Z.7z.001/.002` | 着色器 + VapourSynth + Python + 工具 | 超过 1900MB 自动分卷，GitHub 限制 2GB |
| `03-mpv-fasterwhisper-addon-vX.Y.Z.7z` | AI 语音识别运行时 | 可选，超过 1900MB 自动分卷，GitHub 限制 2GB |
| `04-mpv-lsfg-addon-vX.Y.Z.7z` | 补帧 Layer + 控制脚本 + 内置 Lossless.dll | 仅内置 Lossless.dll，不含 Steam 应用其他文件 |
| `05-mpv-config-vX.Y.Z.7z` | 个人设置最终覆盖层 | 最后安装；不含大型 Logo 素材（归 01） |
| `mpv-full-private-vX.Y.Z.7z` | 合并 01～05 + 完整 Lossless Scaling | **仅本地，禁止公开上传** |
| `VantaInstaller-win-x64-v<安装器版本>.exe` | 三合一辅助安装工具（安装/卸载/设置） | 单文件自包含压缩版，免 .NET 运行时，约 64MB；安装器版本独立于 mpv 包版本 |

## 5. 构建后验证（全部必须通过）

- [ ] 每个归档执行 `7z t` 完整性测试，全部无错误。
- [ ] 计算全部产物的 SHA-256，写入 `version/版本迭代记录.md`。
- [ ] 门禁检查：公开包不得包含 `release/`、`build/`、`tmp/`、`.git/`、`__pycache__`、`.pyc`、`.log`、调试产物等。
- [ ] 公开包除 04 已确认内置的 `Lossless Scaling\Lossless.dll` 外，不得包含其他第三方专有文件；04 包不得包含 `LosslessScaling.exe`、语言资源、.NET/WPF DLL、模型资源等。
- [ ] 01 Base 必须包含 `portable_config/script-assets/`（启动 Logo 素材）；05 Config 必须排除该目录。
- [ ] 01/05 公开包均不得包含 `portable_config/script-opts/window_state.conf`（个人窗口运行时状态）。
- [ ] 核对分卷数量与大小，确认符合 GitHub 上传限制。
- [ ] 构建暂存目录清理（脚本会自动删除 `build/`）。
- [ ] VantaInstaller 单文件 exe 启动验证通过（免 .NET 环境可运行），SHA-256 写入 `version/版本迭代记录.md` 与 Release 说明。

## 6. 提交与标签

推荐提交顺序：

1. 功能/配置改动提交（本次发布内容）。
2. `docs: record vX.Y.Z build, checksums and release plan`：构建结果、校验和、发布计划记录。
3. 创建标签：`git tag vX.Y.Z`（指向发布提交）。
4. 推送：`git push origin master --tags`。

推送后必须验证：

```powershell
git status --short --branch
git log -1 --oneline --decorate
git ls-remote --tags origin vX.Y.Z
```

## 7. 创建 GitHub Release

- [ ] 在 `maxzrb/mpv-vanta-edition` 创建 Release，标签选 `vX.Y.Z`。
- [ ] 状态必须为**正式发布**（非草稿、非预发布）。
- [ ] 上传公开资产：01、02 两个分卷、03、04、05，以及 VantaInstaller 安装器 exe。
- [ ] **不**上传 `mpv-full-private-vX.Y.Z.7z`。
- [ ] Release 说明包含：本次更新内容、安装覆盖顺序、校验和位置、VantaInstaller 安装器说明（用途、免运行时、SHA-256）。
- [ ] 若安装顺序/包结构/下载方式有变化，同步更新远端 README。
- [ ] 上传完成后核对远端资产大小与本地 SHA-256 一致。

## 8. 发布后收尾

- [ ] 提交 `docs: record vX.Y.Z release results`，记录 Release 链接、资产核对结果。
- [ ] 清理 `build/` 暂存目录；`release/` 保留本地产物与校验和记录。
- [ ] `VantaInstaller/publish/` 保留本地安装器产物；安装器源码随仓库提交（`VantaInstaller/**/bin|obj|publish` 不入库）。
- [ ] 更新 `docs/codex/STATUS.md` 快照与日志、`version/工作进度.md`、`version/版本迭代记录.md`。
- [ ] 确认 `master` 与 `origin/master` 同步，工作树干净。

## 9. Agent 强制规则

1. 发布任务开始前必须完整阅读本文件，并把检查结果逐项写入 `docs/codex/STATUS.md`。
2. 任何一项前置检查失败，禁止继续发布，先修复或向用户汇报。
3. 命中“大改动 Gate”（见 3.2）时，**停止发布并汇报**；由用户决定修正发布流程或豁免，agent 不得自行修改本文件或流程。
4. 个人私用全量包任何时候都不得上传公开 GitHub Release。
5. 发布完成后必须按第 8 节收尾，不能只推到 GitHub 就算完成。

## 10. 历史发布参考

| 版本 | 日期 | 包结构 | Release |
|------|------|--------|---------|
| v1.3.2 | 2026-08-04 | 01→02→03→04→05 | https://github.com/maxzrb/mpv-vanta-edition/releases/tag/v1.3.2 |
| v1.3.1 | 2026-07-31 | 01→02→03→04→05 | https://github.com/maxzrb/mpv-vanta-edition/releases/tag/v1.3.1 |
| v1.2.0 | 2026-07-30 | 01→02→03→04 | https://github.com/maxzrb/mpv-vanta-edition/releases/tag/v1.2.0 |
| v1.1.1 | 2026-07-29 | 旧结构 | https://github.com/maxzrb/mpv-vanta-edition/releases/tag/v1.1.1 |
| v1.1.0 | 2026-07-29 | 旧结构 | https://github.com/maxzrb/mpv-vanta-edition/releases/tag/v1.1.0 |
| v1.0.0 | - | 旧结构 | https://github.com/maxzrb/mpv-vanta-edition/releases/tag/v1.0.0 |
